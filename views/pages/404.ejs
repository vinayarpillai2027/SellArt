<%- include('../partials/head') %>
<%- include('../partials/nav', { active: '' }) %>

<main class="page-shell">
  <section class="section section--narrow">
    <div class="empty-state glass-soft">
      <p class="empty-title">We couldn’t locate that page.</p>
      <p class="empty-body">
        It may have been archived or renamed. You can continue exploring the current collection below.
      </p>
      <div class="empty-actions">
        <a href="/" class="btn btn-primary">Return home</a>
        <a href="/shop" class="btn btn-ghost">Browse gallery</a>
      </div>
    </div>
  </section>
</main>

<%- include('../partials/footer') %>

